# AP Retail POS — Interface Design Plan

## Product intent

AP Retail POS เป็นหน้าร้านและเครื่องขายหน้าร้านสำหรับผู้ประกอบการที่เชื่อมข้อมูลสินค้า ราคา สต๊อก และคำสั่งซื้อกับ AP Service กลาง โดยเว็บแอปเป็นผลิตภัณฑ์หลัก ส่วน Android shell เปิดเว็บเดียวกันใน WebView เพื่อไม่ให้ข้อมูลและหน้าจอแยกคนละเวอร์ชัน

## Portrait mobile principle

ทุกหน้าสำคัญออกแบบสำหรับมือถือแนวตั้ง 9:16 และการใช้งานมือเดียว โดยใช้แถบนำทางล่าง, ปุ่มหลักสูงอย่างน้อย 48px, ฟิลด์ค้นหาที่เข้าถึงง่าย และ bottom sheet สำหรับการยืนยันรายการขาย ไม่แสดงยอดขายหรือสต๊อกจำลอง: เมื่อไม่พบข้อมูลต้องแสดงสถานะว่างที่อธิบายได้

## Screen list

| Screen | Primary content and functionality |
|---|---|
| Sign in | เข้าสู่ระบบด้วยบัญชีร้านที่มีอยู่และตรวจ role ก่อนเปิดร้าน |
| POS Register | ค้นหา/สแกนสินค้า, กรองหมวดหมู่, ตะกร้าขาย, ปรับจำนวน, เลือกวิธีรับเงิน และสร้างรายการขายผ่าน RPC |
| Product catalog | สินค้าของสาขาที่ใช้งาน, สถานะพร้อมขาย, ราคา, จำนวนคงเหลือที่อนุญาตให้เห็น และทางลัดแก้ไข |
| Product editor | เพิ่ม/แก้ไขรายการสินค้า, SKU, ราคา, หน่วย, หมวดหมู่, ภาพจากกล้อง/คลัง พร้อม compression max 1200px JPEG 0.82 |
| Inventory | ยอดคงเหลือจริง, รับเข้า, ปรับแก้โดยต้องระบุเหตุผล, low-stock state และประวัติการเคลื่อนไหว |
| Sales history | การขายของสาขา, สถานะชำระเงิน, รายละเอียดรายการ และการคืนสินค้าเฉพาะ policy ที่อนุญาต |
| Store settings | รายละเอียดสาขา เวลาทำการ และสถานะเปิดขาย โดยไม่ซ้ำข้อมูลที่ Admin เป็นเจ้าของ |
| Account | ข้อมูลบัญชีผู้ใช้, ออกจากระบบ และจุดช่วยเหลือ |

## Key user flows

1. ผู้ขายเข้าสู่ระบบ → ระบบตรวจ role ร้านค้า → เลือก/คืนค่าสาขาที่มีสิทธิ์ → เข้าหน้า POS Register
2. ผู้ขายค้นหาหรือเลือกสินค้า → เพิ่มตะกร้า → ปรับจำนวนภายในสต๊อกที่ระบบยืนยัน → เลือกวิธีชำระ → ยืนยัน → RPC สร้าง sale, sale item และ inventory movement แบบ atomic → แสดงเลขอ้างอิงจริง
3. ผู้ขายกดสินค้าใน Catalog → แก้ไขข้อมูล → หากอัปโหลดรูป เลือกกล้อง/คลังเท่านั้น → บีบอัดภาพ → บันทึก → สินค้าอัปเดตใน POS และ Customer catalog ตามกฎ visibility
4. ผู้ขายเปิด Inventory → รับเข้า/ปรับยอด → ระบุเหตุผล → ระบบสร้าง inventory movement ที่ตรวจสอบย้อนหลังได้ → คงเหลือใหม่แสดงจากข้อมูลจริง

## Visual direction

แบรนด์ใช้สี **เขียวมรกต `#12805C`**, สีเน้นมิ้นต์ `#DDF7EE`, ครีม `#F8F5ED`, ส้มอำพันสำหรับสถานะระวัง `#F4A340`, และสีข้อความเข้ม `#17352B`. การ์ดมีมุมโค้ง 20px, เงานุ่ม, ปุ่มแบบ pill ที่อ่านง่าย และไอคอน line-style. ต้องคงความชัดของตัวอักษรและ contrast สำหรับผู้ใช้ทุกวัยตามแนวทาง HIG.

## Android shell

Android APK เปิด URL ของ GitHub Pages หลัง deployment ผ่าน WebView พร้อม allow-list เฉพาะโดเมน AP Retail POS, รองรับ loading/error/retry และไม่ฝัง Supabase key ที่มีสิทธิ์สูงใน native code. UI และข้อมูลหลักต้องมาจากเว็บแอปเดียวเพื่อให้รุ่น APK และเว็บสอดคล้องกัน.
